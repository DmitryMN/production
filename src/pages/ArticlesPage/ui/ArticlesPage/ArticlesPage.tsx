import React from 'react';

interface ArticlesPageProps {
  clasName?: string;
}

const ArticlesPage: React.FC<ArticlesPageProps> = ({ clasName }) => {
  return (
    <div>ArticlesPage 111</div>
  );
};

export default ArticlesPage;
